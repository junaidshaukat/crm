# run this command on root path of project

```
keytool -genkey -v -keystore keystore.jks -storetype JKS -keyalg RSA -keysize 2048 -validity 10000 -alias release
```
![keystore](screenshot/keystore.png)

# also change these keys
![key.properties](screenshot/key.properties.png)
